#include <iostream>
using namespace std;

int main()
{
	int a, b;
	cout << " ikkita sonni kiriting";
	cin >> a >> b;
	if (b) cout << a / b << '/n';
	if (b) cout << a * b << '/n';
	if (b) cout << a - b << '/n';
	if (b) cout << "nolga bolish mumkin emas/ n";
	return 0;

}
